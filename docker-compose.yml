version: "3"

services:
  database:
    image: mysql:5.7
    container_name: db
    environment:
      - MYSQL_DATABASE=blog
      - MYSQL_ROOT_HOST=%
      - MYSQL_ROOT_PASSWORD=tkacns11
    ports:
      - 3306:3306
      
  application:
        build: .
        ports:
          - 8080:8080
        depends_on:
          - database
    
 